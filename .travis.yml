language: python
python: 
  - 2.7
  - 3.6

# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt
  - pip install -e .

# command to run tests
script:
  - pyflakes mozilla_bitbar_devicepool/*.py
  - pytest mozilla_bitbar_devicepool
